# Screenshot Fixtures

`scripts/capture_va00_screenshots.sh` renders the visual foundation directly at 320×180. Generated captures are ignored; reviewed 1× references belong under `raw_320x180/`. Enlarged review copies must live in an importer-ignored preview directory.
