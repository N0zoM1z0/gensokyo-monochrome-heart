# Development Placeholder Art

Placeholder art must be project-original geometry, use the `ph_` filename/ID prefix, remain limited to development or QA, and have no ambiguous provenance. The release export excludes this directory, while the release scanner rejects any placeholder that leaks into another runtime path.
