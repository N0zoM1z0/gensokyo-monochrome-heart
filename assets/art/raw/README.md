# Raw Runtime Art

Only 1× source assets belong here. Every PNG must pass the strict black/white and binary-alpha validator before import. Enlarged previews and concept images belong in ignored preview directories and are never release inputs.
