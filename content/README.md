# Runtime Content

This directory contains reviewable runtime source data. Starter fixtures are synchronized deterministically from `design/09_data`; implementation-owned records are added only with stable IDs, schemas, localization, and review provenance.

Generated indexes live in `content/indexes/` and must be reproducible.
