# Content Schemas

The JSON Schemas in this directory are synchronized byte-for-byte from `design/09_data/schemas/` by `scripts/sync_design_content.py`. Edit the pinned design source or add a reviewed implementation schema; do not hand-edit a synchronized copy.
